rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: Permissive rules for testing ONLY.
    // These rules allow all reads and writes to every document. Do NOT use this in production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
